# Life Chronicles - Environment Variables Template
# Copy this file to .env (local development) or configure in Render dashboard (production)

# ========================================
# DATABASE CONFIGURATION
# ========================================

# For Render PostgreSQL, get these values from your database's "Info" tab
# Example: postgres://user:password@hostname:port/database

DB_HOST=localhost
DB_PORT=5432
DB_NAME=lifechronicles
DB_USER=postgres
DB_PASSWORD=postgres

# ========================================
# RENDER POSTGRESQL EXAMPLE
# ========================================
# When deploying to Render, use these values from your Render PostgreSQL service:
#
# DB_HOST=dpg-xxxxxxxxxxxxx-a.oregon-postgres.render.com
# DB_PORT=5432
# DB_NAME=your_database_name
# DB_USER=your_database_user
# DB_PASSWORD=<render-generated-password>
#
# Note: Render provides these automatically in the "Info" tab of your PostgreSQL service

# ========================================
# APPLICATION CONFIGURATION (Optional)
# ========================================

# Session timeout in minutes (configured in web.xml, default: 30)
# SESSION_TIMEOUT=30

# Application port (Render sets this automatically)
# PORT=8080
